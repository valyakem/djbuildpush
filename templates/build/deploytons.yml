jobs:
- job: Test_Kubernetes_Command
  pool:
    name: $(agent-pool-name)

  steps:
   #creation of client's secret in kubernetes
  - task: AzureCLI@2
    displayName: Install_customers_kubernetes_files
    inputs:
      azureSubscription: '$(azure-subscription)'
      scriptType: 'bash'
      scriptLocation: 'inlineScript'
      inlineScript: |
        docker login -u nbregtoken -p Vpacm6aGugMe2Vj5t6d9iVRfkTWiKftnmx0icMUeKG+ACRAahlsj nbreg.azurecr.io
        kubectl create -R -f templates/tenants/capitos/ -n usermanagement



