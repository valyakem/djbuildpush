apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: web-server-ingress1
  namespace: matello-tech
spec:
  ingressClassName: nginx
  rules:
  - host: nbapp1.nexgbits.com
    http:
      paths:
      - path: /web
        pathType: Prefix
        backend:
          service:
            name: web-server-service1
            port:
              number: 8000